import type{JsonValue}from"@/features/individual-session/types";import{selfReplacements,type IdentityCheckpoint,type IdentityJudgment,type ReplaceYourselfResponseState}from"./replace-yourself.types";export const checkpointCounts=[2,4,6,8,10]as const;export const initialReplaceYourselfState:ReplaceYourselfResponseState={version:1,stage:"transforming",replacedCount:0,checkpoints:[]};export function recordJudgment(state:ReplaceYourselfResponseState,judgment:IdentityJudgment){const checkpoint:IdentityCheckpoint={after:state.replacedCount,judgment};return{...state,checkpoints:[...state.checkpoints.filter(item=>item.after!==state.replacedCount),checkpoint]}}export function firstJudgmentChange(checkpoints:IdentityCheckpoint[]){const first=checkpoints[0]?.judgment;return checkpoints.find(item=>item.judgment!==first)?.after??null}export function physicalPsychologicalPattern(state:ReplaceYourselfResponseState){const physical=state.checkpoints.filter(item=>item.after<=6);const psychological=state.checkpoints.filter(item=>item.after>6);return{physicalNotSame:physical.some(item=>item.judgment==="not-same"),psychologicalNotSame:psychological.some(item=>item.judgment==="not-same")}}export function parseReplaceYourselfState(value:JsonValue|null):ReplaceYourselfResponseState{if(value===null||typeof value!=="object"||Array.isArray(value))return initialReplaceYourselfState;const stage=value.stage,cps=value.checkpoints;if(value.version!==1||!(stage==="transforming"||stage==="reflection"||stage==="completed")||!Number.isInteger(value.replacedCount)||Number(value.replacedCount)<0||Number(value.replacedCount)>selfReplacements.length||!Array.isArray(cps)||!cps.every(cp=>cp!==null&&typeof cp==="object"&&!Array.isArray(cp)&&checkpointCounts.includes(cp.after as typeof checkpointCounts[number])&&(cp.judgment==="same"||cp.judgment==="not-same"||cp.judgment==="unsure")))return initialReplaceYourselfState;return{version:1,stage,replacedCount:Number(value.replacedCount),checkpoints:cps as unknown as IdentityCheckpoint[]}}export function serializeReplaceYourselfState(state:ReplaceYourselfResponseState):JsonValue{return{...state,checkpoints:state.checkpoints.map(item=>({...item}))}}
