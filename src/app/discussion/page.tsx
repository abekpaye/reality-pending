import type{Metadata}from"next";import Link from"next/link";import{ApplicationShell}from"@/components/layout/application-shell";import{discussions}from"@/data/discussion";import{getExperiment}from"@/features/individual-session/registry";import styles from"../editorial-reference.module.css";export const metadata:Metadata={title:"Seminar Discussion — REALITY PENDING"};export default function DiscussionPage(){return <ApplicationShell mode="standard" label="REALITY PENDING / DISCUSSION"><main className={styles.page}><header><p>SEMINAR DISCUSSION</p><h1>Questions that begin after the decision.</h1><span>Seven prompts, each with pressure and variation.</span></header><div className={styles.entries}>{discussions.map((item,index)=>{const experiment=getExperiment(item.experimentId);return <article key={item.experimentId}><span>{String(index+1).padStart(2,"0")}</span><div><p className={styles.kicker}>{experiment.shortTitle}</p><h2>{item.prompt}</h2><dl><div><dt>FOLLOW-UP CHALLENGE</dt><dd>{item.challenge}</dd></div><div><dt>COUNTERFACTUAL</dt><dd>{item.counterfactual}</dd></div></dl></div></article>})}</div><footer><Link href="/concepts">Explore concepts</Link><Link href="/">Return to REALITY PENDING</Link></footer></main></ApplicationShell>}
