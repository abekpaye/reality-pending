# Individual Mode does not require environment variables.
# Class Mode uses only Supabase's public browser credentials. Never place a service-role key here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
